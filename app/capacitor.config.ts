import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.blockchain.dapp',
  appName: 'Blockchain dApp',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;